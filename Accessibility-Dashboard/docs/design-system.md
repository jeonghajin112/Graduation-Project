# 디자인 시스템

현재 UI를 유지·확장할 때의 기준이다. 실제 토큰 값과 컴포넌트 props는 코드가 원본이며 이 문서는 역할과 사용자 동작을 설명한다.

## 스타일을 찾는 위치

| 범위 | 구현 | 적용 원칙 |
|---|---|---|
| 공용 의미 토큰·폰트·포커스 | [index.css](../src/index.css) | `background`, `foreground`, `card`, `primary`, `border`, `ring` 등 역할 이름 사용 |
| 대시보드 테마·상태색 | [theme-tokens.css](../src/styles/theme-tokens.css) | 공용 의미를 화면 역할로 매핑 |
| 페이지 상세 레이아웃 | [page-evidence-layout.css](../src/styles/page-evidence-layout.css) | 컨테이너 크기와 확대에 맞춰 배치 |
| 재사용 입력·대화상자·차트 | [components/ui](../src/components/ui/) | 기존 variant와 접근성 동작을 확인한 뒤 확장 |
| 대시보드 모달 | [dashboard-modals.css](../src/styles/dashboard-modals.css) | 표면·제목·입력·버튼·알림 역할을 공유하며 문서 루트의 테마 토큰으로 portal에도 동일하게 적용 |
| 랜딩 | `.uni-landing`과 `--ua-*` 토큰 | 대시보드 전역 스타일과 범위 분리 |

색상 hex, variant 목록, 모든 breakpoint를 문서에 복사하지 않는다. 동일한 역할의 값을 화면마다 새로 만들지 않고, 반복되는 예외만 공용 토큰이나 variant로 올린다.

## 읽기와 배치

기본 서체는 Pretendard 계열이며 일반 컨트롤·본문은 14px를 기준으로 한다. 한국어 줄바꿈과 긴 URL을 고려하고 수치 비교에는 `tabular-nums`를 사용한다.

대시보드는 조밀한 작업 화면이다. 새 UI는 주요 간격 12–16px, 주요 조작 대상 44×44px, 컨트롤 반경 8px를 출발점으로 삼는다. 실제 컨테이너와 정보량에 맞춰 조정한다. 기존의 작은 트리 아이콘이나 화면별 큰 카드 반경을 새 공용 기준으로 복사하지 않는다.

페이지 상세의 미표시 문제 패널은 카드 높이를 유지하고, 마지막 카드 바로 아래에 인디케이터와 패널 하단을 배치한다. 페이지당 표시 개수는 오른쪽 영역의 가용 높이로 계산하므로 패널이 작아져도 표시 용량이 줄지 않으며, 마지막 페이지에 카드가 적어도 남은 높이를 채우려고 늘리지 않는다.

데스크톱 페이지 상세는 사이드바를 제외한 영역의 좌우 여백을 24px로 두고 가용 폭을 채운다. 오른쪽 분석 카드의 기존 반응형 폭은 유지하고, 여백에서 확보한 공간은 렌더링 화면에 배분한다.

페이지 검사 화면 상단은 해당 사이트의 파비콘과 라이브 문서의 브라우저 탭 제목(`document.title`)을 담은 얇은 반투명 바를 사용한다. 프로젝트에서 지정한 페이지 이름을 대신 표시하지 않는다. 문서 제목을 받기 전에는 제목을 불러오는 중임을 표시하고, 실제 제목이 비어 있으면 제목 없음으로 표시한다. 주소를 제목 대신 표시하지 않는다. 파비콘이 없거나 로드에 실패하면 중립색 웹 아이콘을 표시하고, 긴 제목은 말줄임 처리한다. 닫기 버튼은 두지 않는다. 문서 맨 위에서는 불투명한 기본 상단바가 높이를 확보하고, 원본 페이지는 그 아래에서 시작한다. 내부 페이지를 스크롤하면 일반 본문은 반투명 상단바 뒤로 지나가며 흐려진다. 브리지는 상단바의 실제 높이와 화면 축소 비율을 받아 위쪽에 고정된 메뉴·로고의 위치를 보정하고, 마커와 설명창도 가려진 영역 아래에 표시한다. 카드 전체 높이는 유지하며 맨 위로 돌아오면 메뉴의 원래 인라인 스타일과 기본 상단바 배치를 복원한다. 렌더링 화면은 흰색 외곽 여백 없이 카드 끝까지 채우며 바깥 카드만 모서리를 자른다. 반투명 배경과 흐림 스타일을 유지하되 페이지 내용을 가리지 않는 배치를 우선하고, 강제 색상 모드에서는 불투명하게 표시한다.

고정 메뉴 보정은 사이트 주소나 특정 선택자에 의존하지 않고 실제 `fixed`·`sticky` 배치를 기준으로 한다. 작은 플로팅 메뉴와 독립된 여러 행은 간격을 유지하며, 중첩 메뉴에는 높이를 중복 적용하지 않는다. 동적 삽입·스타일 변경·반응형 전환과 열린 Shadow DOM 내부도 추적한다. 처음에는 문서를 탐색하고 이후에는 변경된 부분만 갱신해, 스크롤마다 전체 본문을 다시 탐색하지 않는다. 닫힌 Shadow DOM 등 브라우저에서 접근할 수 없는 내부 구조는 보정 대상에 포함할 수 없다.

색만으로 심각도·선택·성공·실패를 표시하지 않는다. 레이블, 수치, 아이콘 또는 패턴을 함께 제공한다. 차트의 중요한 값은 hover 툴팁 없이도 읽을 수 있어야 한다.

## 테마와 모션

최근 분석 날짜와 재분석 버튼은 렌더링 화면 상단바의 우측 끝에 한 묶음으로 표시한다. 좁은 화면에서도 날짜와 버튼을 유지하고 홈페이지 제목을 말줄임 처리한다.

페이지 주소는 상단바 중앙의 별도 박스에 표시하고, 새 탭 열기와 텍스트 선택·복사를 지원한다. 좁은 화면에서는 제목·재분석 버튼 아래 행에 중앙 정렬한다.

오른쪽 정보 영역은 최근 분석 추이부터 표시하며 별도의 페이지 정보 카드를 두지 않는다. 재분석 실패와 화면 정보 로딩·재시도 안내는 해당 상태일 때만 표시한다.

라이브 리포트 상단바는 운영체제의 투명도 감소 설정에서도 스크롤 시 유리 효과를 유지한다. 이 예외는 해당 상단바에만 적용하며, 강제 색상 모드에서는 대비를 위해 불투명 배경을 사용한다.

라이브 리포트 로딩 중에는 상단바와 본문을 하나의 카드 배경과 얇은 테두리로 연결한다. 테두리는 로딩 완료 시 사라지며 렌더링 영역의 크기에 영향을 주지 않는다.

최근 분석한 페이지는 우클릭 또는 `Shift+F10`으로 삭제 메뉴를 연다. 확인 후 기존 페이지 삭제 API를 호출하며, 프로젝트에 속한 페이지는 프로젝트에서도 제거됨을 안내한다. 실패 시 목록을 유지하고 재시도를 제공한다. 읽기 전용 미리보기에는 삭제 메뉴를 제공하지 않는다.

light·dark·system에서 같은 정보를 제공한다. 문서 루트의 `.dark`와 대시보드의 `theme-light/theme-dark` 적용을 함께 확인한다. 저장소를 쓸 수 없어도 현재 세션에서 테마가 동작해야 한다.

대시보드의 추가·수정·제거·설정·문제 상세와 로딩·오류 모달은 공통 모달 클래스를 사용한다. 일반 작업 버튼은 파란색, 취소·닫기는 중립색, 실제 삭제 동작은 위험 색상을 사용한다. 버튼은 각 화면의 기존 작은 크기를 유지하며 색상 통일을 위해 일괄 확대하지 않는다. 내용에 필요한 폭과 스크롤 구조는 각 모달에서 유지한다.

모션은 상태 변화를 이해하는 데 필요한 만큼 사용한다. `prefers-reduced-motion`에서는 반복·이동 효과 없이도 상태를 알 수 있게 한다. 투명도 축소와 forced colors에서는 표면·경계·포커스를 구분할 수 있어야 한다.

1920px보다 넓은 화면에서는 메시지·문제 확인·분석 엔진·FAQ·푸터의 콘텐츠 폭, 글씨와 여백을 함께 확대한다. `landing-large-screen.css`는 FHD 비율을 기준으로 화면 폭과 높이에 따라 최대 2배까지 확장하며, 낮은 울트라와이드 창에서는 높이로 확대량을 제한한다. 기존 모바일 배치와 스크롤 단계, 렌더링 화면의 16:9 비율은 유지한다.

랜딩의 프로젝트 소개와 분석 엔진 사이에는 세 줄 메시지를 가운데 정렬한다. 문구가 화면에 들어오면 위에서부터 순서대로 나타나고, 영역을 벗어났다가 다시 들어오면 재생한다. 모션 감소 설정에서는 세 줄 모두 즉시 표시한다.

랜딩의 페이지 보기 장면은 실제 서비스의 페이지 영역만 녹화해 보여준다. 사이드바·점수 영역은 녹화 범위에서 제외하고, 문제 설명창을 열지 않은 채 실제 페이지를 스크롤한다. `record:landing -- --report-only --publish`는 기존 분석 결과로 이 장면만 교체하며 새 분석을 요청하지 않는다.

세 줄 메시지 다음에는 실제 서비스의 마커 설명 화면과 함께 문제 위치·판단 이유·개선 안내를 소개한다. 페이지 보기 장면에서 이어지는 결과 설명이며, 문장·색상 제안은 제공되는 항목에 한해 안내한다. 화면 캡처는 지연 로드하며 모션 감소 설정에서는 내용을 즉시 표시한다.

문제 확인과 개선 섹션은 제목과 실제 렌더링 화면이 한 장면으로 등장한다. 스크롤을 내리면 왼쪽 제목이 사라지고 같은 자리에 세 항목이 나타나며, 이후 위치·이유·개선 방향을 순서대로 표시한다. 구간 안에서는 설명과 실제 캡처를 함께 고정하고 단계에 맞춰 확대 영역을 바꾼다. 역방향 스크롤에서는 이전 단계로 돌아가며, 구간이 끝나면 다음 섹션으로 이어진다. 단계는 클릭 버튼이 아닌 현재 단계 표시이고, 모바일에서도 같은 순서로 진행한다. 현재 단계의 설명만 부드럽게 펼쳐지고 이전 설명은 접힌다. 펼침 높이가 달라져도 스크롤 전환 지점은 유지한다. 모션 감소 설정에서는 확대 상태를 이동 애니메이션 없이 적용한다.

라이브 리포트의 마커를 선택해도 검정 배경과 심각도 색상을 유지한다. 요소 테두리도 심각도 색상을 유지하며 선택 시 파란색으로 전환하거나 광택 테두리를 추가하지 않는다. 설명창 열기와 키보드 포커스 표시는 유지한다.

랜딩 마지막에는 총점 구성·등급 카드 대신 질문과 답변을 제공한다. 왼쪽 소개와 오른쪽 질문 목록을 배치하고 모바일에서는 세로로 쌓는다. 질문은 기본 접힌 상태의 `details`·`summary`로 제공하며 마우스와 키보드로 답변을 열고 닫을 수 있다. 답변은 서비스 이용 방법, 분석 범위, 수동 수정, 자동 검사의 한계, 실제 촬영 화면의 의미를 설명한다.

FAQ 다음에는 로고·서비스 소개·연도 표시를 담은 푸터를 둔다. 로고는 페이지 상단으로 이동하며 본문에 포커스를 돌려준다. 모션 감소 설정에서는 즉시 이동하고, 고정 TOP 버튼을 가리지 않도록 하단 여백을 확보한다.

빠른 분석의 대기 화면은 페이지 도식과 세로 단계 안내를 나란히 배치한다. 좁은 컨테이너에서는 읽을 수 있는 진행 안내를 먼저 두고 도식을 아래에 배치한다. 스캔선은 실제 검사 중에만 움직이며, 완료·오류·상태 확인 대기에서는 멈춘다. 도식은 장식으로 처리하고 현재 단계는 텍스트와 status 알림으로 전달한다. 진행률이나 남은 시간을 임의로 생성하지 않는다.

## 접근성 동작

대시보드와 모달의 제목·버튼·상태 배지·점수는 텍스트 드래그 선택을 하지 않는다. 주소, 이슈 설명·원문·요소 경로·HTML·좌표, 오류 안내와 입력 필드는 선택·복사할 수 있다. 추가로 복사할 내용은 `data-copyable`로 표시한다. CSS로만 선택 범위를 구분하며 키보드 조작이나 iframe 안의 원본 웹페이지 선택을 차단하지 않는다. 이슈 설명을 드래그한 뒤에는 카드 페이지가 넘어가지 않는다.

- 키보드 조작 순서와 읽기 순서는 화면 폭이 바뀌어도 의미가 같아야 한다.
- 본문 바로가기, 현재 위치의 `aria-current`, 아이콘 버튼의 접근성 이름을 유지한다.
- 포커스를 보이게 하고 오류 입력에는 `aria-invalid`와 읽을 수 있는 오류 설명을 연결한다.
- 로딩·비파괴 갱신은 status/live region, 즉시 알려야 하는 오류는 해당 오류 의미로 전달한다.
- 대화상자는 제목, 열릴 때 포커스 이동, Tab 순환, 허용된 Escape 닫기, 닫힌 뒤 포커스 복원을 제공한다.
- 실제 0, 자료 없음, 확인 중, 실패를 구분한다. 연결된 리포트와 위치 확인 완료를 같은 상태로 처리하지 않는다.

프로젝트의 UI 검증 목표는 일반 텍스트 대비 4.5:1, 큰 텍스트 3:1, 주요 UI·포커스 3:1이다. 화면 변경 범위에 맞게 모바일·데스크톱, 200% 확대, 긴 한국어·URL을 확인한다. 표준 적합성 전체를 이 수치 검사만으로 단정하지 않는다.

## 기존 컴포넌트의 제한

- Sidebar는 모든 화면 크기에서 영구 rail을 사용하며, `SidebarBody`로 렌더링한다.
- 기본 차트 툴팁의 0값 표시와 분석 추이의 빈 슬롯은 별도로 확인해야 한다. 추이 오류는 [아키텍처의 현재 오류](architecture.md)에 기록되어 있다.
- 일부 상태 패널에는 전용 색상이 남아 있다. 정리할 때 의미·포커스·알림 동작을 함께 보존한다.

이 제한을 문서에서 제거하는 것으로 해결된 것으로 처리하지 않는다.

## 변경 확인

기존 토큰·컴포넌트를 먼저 확인하고 변경 범위의 hover, focus, disabled, loading, empty, error를 검증한다. 공용 동작 변경은 호출부와 함께 수정한다.

실행할 검사는 [테스트 가이드](testing.md)에서 고른다. 전역 UI·대화상자 변경에는 접근성 회귀, 랜딩 변경에는 해당 시각 검증, 데이터와 차트 계산 변경에는 단위·화면 검증을 추가한다. 작은 시각 변경에 관계없는 전체 검증을 반복하지 않는다.
